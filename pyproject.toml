[project]
name = "statteluq"
version = "0.1.0"
description = "Explorateur interactif des inscriptions TELUQ"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.35",
    "plotly>=5.20",
    "duckdb>=0.10",
    "pandas>=2.0",
    "kaleido>=0.2.1",
]

[tool.uv]
# uv-specific settings (optional)
# dev-dependencies can be added here under [tool.uv.dev-dependencies] if needed later
